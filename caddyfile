{
  admin off
  auto_https off
  persist_config off
  log {
    format json
  }
}

:{$PORT} {
  log {
    format json
	}
  rewrite /health /*
  root * {$ANGULAR_OUTPUT_PATH}
  encode gzip
  file_server
  try_files {path} /index.html
}
